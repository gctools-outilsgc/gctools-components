/* eslint-disable global-require */
import Container from './Container';

export const DataProvider = require('./dataProvider').default;
export const EventHandler = require('./eventHandler').default;

export default Container;
